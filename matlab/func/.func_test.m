function [hasil1] = func_cobaKaliTambah(in1,in2)
    % create custom help
    
    a = in1+in2;
    b = in1+in2;
    hasil1 = a*b;
    
    % while / for loop + break
    % ...
    % if else
    % ...
end
